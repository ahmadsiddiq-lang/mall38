import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default function EditAlamat() {
    return (
        <View style={styles.Container}>
            <Text>EditAlamat</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    Container: {
        flex: 1,
    },
});
